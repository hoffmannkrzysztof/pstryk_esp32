# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# Secrets (if ever compiled-in instead of provisioned)
src/secrets.h
secrets.h

# Brainstorm visual-companion session files (not shipped)
.superpowers/

# Git worktrees (isolated feature workspaces)
.worktrees/

# Editor / OS
.vscode/
.DS_Store
